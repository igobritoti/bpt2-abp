name: BPT2 Product API Gate

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

on:
  pull_request:
    paths:
      - 'modules/catalog/**'
      - 'modules/sellers/**'
      - 'modules/marketplace/**'
      - 'modules/ingestion/**'
      - 'main/**'
      - 'tests/BomPraTi.HttpLifecycleFixture/**'
      - 'scripts/product-api-smoke.sh'
      - 'scripts/ingestion-candidate-http-smoke.sh'
      - 'scripts/ingestion-vehicle-lookup-http-smoke.sh'
      - 'scripts/admin-operations-hub-http-smoke.sh'
      - 'scripts/admin-operations-summary-http-smoke.sh'
      - 'scripts/admin-global-navigation-http-smoke.sh'
      - 'scripts/fresh-migration-gate.sh'
      - '.github/workflows/product-api-gate.yml'

permissions:
  contents: read

jobs:
  product-api:
    runs-on: ubuntu-24.04
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: BomPraTi
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d BomPraTi"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      BPT_DB_CONNECTION: Host=localhost;Port=5432;Database=BomPraTi;Username=postgres;Password=postgres
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: '10.0.x'
      - name: Syntax check product HTTP smokes
        run: |
          bash -n scripts/product-api-smoke.sh
          bash -n scripts/ingestion-candidate-http-smoke.sh
          bash -n scripts/ingestion-vehicle-lookup-http-smoke.sh
          bash -n scripts/admin-operations-hub-http-smoke.sh
          bash -n scripts/admin-operations-summary-http-smoke.sh
          bash -n scripts/admin-global-navigation-http-smoke.sh
      - name: Apply fresh database
        run: bash scripts/fresh-migration-gate.sh
      - name: Seed host identity and OpenIddict data
        shell: bash
        run: |
          set -euo pipefail
          export ConnectionStrings__Default="$BPT_DB_CONNECTION"
          export ASPNETCORE_ENVIRONMENT=Development
          cd main/BomPraTi
          dotnet run --configuration Release --no-build -- --migrate-database
      - name: Seed canonical Vehicle fixture
        shell: bash
        run: |
          set -euo pipefail
          vehicle_id="$(dotnet run --project tests/BomPraTi.HttpLifecycleFixture/BomPraTi.HttpLifecycleFixture.csproj --configuration Release | tail -n 1)"
          test -n "$vehicle_id"
          echo "BPT_FIXTURE_VEHICLE_ID=$vehicle_id" >> "$GITHUB_ENV"
      - name: Verify real product API surfaces
        run: bash scripts/product-api-smoke.sh
      - name: Exercise Ingestion candidate reconciliation
        run: bash scripts/ingestion-candidate-http-smoke.sh
      - name: Exercise Ingestion Vehicle lookup
        run: bash scripts/ingestion-vehicle-lookup-http-smoke.sh
      - name: Exercise admin operations hub
        run: bash scripts/admin-operations-hub-http-smoke.sh
      - name: Exercise admin operations summary
        run: bash scripts/admin-operations-summary-http-smoke.sh
      - name: Exercise admin global navigation
        run: bash scripts/admin-global-navigation-http-smoke.sh
